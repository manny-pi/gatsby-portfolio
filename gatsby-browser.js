/*
`gatsby-browser` is for responding to Gatsby-specific events within the browser,
and wrapping page components in additional global components. 

The Gatsby Browser API provides many options for interacting with the client-side of Gatsby.

For example,

`import "./src/styles/global.css"` eliminates the need to import CSS modules into each of 
components
*/

import "./src/styles/global.css";
